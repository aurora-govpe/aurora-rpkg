% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dockerfile.R
\name{aurora_dockerfile}
\alias{aurora_dockerfile}
\title{Generate a Dockerfile (and .dockerignore) for an aurora app}
\usage{
aurora_dockerfile(
  dir = ".",
  flavor = c("debian", "alpine"),
  base = NULL,
  sysdeps = "auto",
  port = 8000L,
  aurora_source = "aurora-govpe/aurora-rpkg",
  write = TRUE
)
}
\arguments{
\item{dir}{App directory.}

\item{flavor}{Base-image flavor: \code{"debian"} (rocker + PPM binaries) or
\code{"alpine"} (r-minimal + source builds).}

\item{base}{Base Docker image. \code{NULL} (default) resolves per \code{flavor}:
\verb{rocker/r-ver:4.4.1} (debian) or \code{rhub/r-minimal} (alpine).}

\item{sysdeps}{\code{"auto"} uses a curated default set of system packages for the
flavor (covers the plumber2 + bslib baseline plus common TLS/curl/db/geo/
graphics needs); or pass a character vector to set them explicitly. For
\code{"alpine"} this is the \strong{build} (\code{-t}) set; runtime libs use a curated
default. (\code{pkg_sysreqs()} auto-resolution proved unreliable, so aurora ships
comprehensive defaults instead -- extra \code{-dev} packages are build-time only.)}

\item{port}{Port exposed and bound (via the \code{AURORA_PORT} env var).}

\item{aurora_source}{pak/remotes spec used to install aurora in the image.
Pin a tag or commit for reproducible builds (e.g.
\code{"aurora-govpe/aurora-rpkg@v0.1.0"}): an unpinned moving ref (a branch)
interacts badly with Docker's layer cache, which can silently keep an old
commit on rebuild.}

\item{write}{Write the file (\code{TRUE}) or return its text (\code{FALSE}).}
}
\value{
The Dockerfile path (if written) or its contents, invisibly.
}
\description{
Writes a Dockerfile that installs system deps, R packages (incl. \pkg{plumber2}
and \pkg{aurora}), copies the app, and runs \code{api.R}. The image suits plain
Docker or a ShinyProxy container. A \code{.dockerignore} is written if absent.
}
\details{
Two flavors:
\itemize{
\item \code{"debian"} (default) -- \code{rocker/r-ver}; installs R packages as \strong{binaries}
from Posit Package Manager (fast builds; amd64). Best for heavy/geo apps and
fast CI. Larger image.
\item \code{"alpine"} -- \code{rhub/r-minimal}; a tiny image (~25 MB base) that \strong{compiles}
every package from source via \code{installr} (no CRAN binaries; builds are
slower). Best when image size matters or you need native arm64. Tune the
Alpine build/runtime system deps via \code{sysdeps} if your packages need more.
}

Runtime R packages are detected by scanning \verb{routers/}, \verb{helpers/}, and
\code{api.R}. To pin them explicitly (reproducible images), set a \verb{packages:} list
in \verb{_aurora.yml}; \code{plumber2} and \code{aurora} are always added.
}
