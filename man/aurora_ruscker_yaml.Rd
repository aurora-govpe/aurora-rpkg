% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruscker.R
\name{aurora_ruscker_yaml}
\alias{aurora_ruscker_yaml}
\title{Emit a Ruscker app-spec block for an aurora image}
\usage{
aurora_ruscker_yaml(
  image,
  dir = ".",
  id = NULL,
  display_name = NULL,
  description = NULL,
  port = 8000L,
  docs_path = "/__docs__",
  health_path = "/__healthz__",
  rate_limit = NULL,
  cors = NULL,
  min_replicas = 0L,
  max_replicas = 3L,
  env = NULL,
  wrap = FALSE,
  write = FALSE,
  file = "ruscker-app.yml"
)
}
\arguments{
\item{image}{Container image tag, e.g. \code{"org/meu_app:latest"} (required).}

\item{dir}{App directory, used only to default \code{id}/\code{display_name} from the
app name.}

\item{id}{Spec id. Defaults to the app name.}

\item{display_name}{Human-facing name. Defaults to the app name.}

\item{description}{Optional one-line description.}

\item{port}{Port the app listens on inside the container, emitted as
\code{api.port} (the aurora default is \code{8000}).}

\item{docs_path}{OpenAPI/Swagger UI location, emitted as \code{api.docs-path}.
Defaults to \code{"/__docs__"} (the 'plumber2'/aurora default).}

\item{health_path}{Readiness-check endpoint, emitted as \code{api.health-path}.
Defaults to \code{"/__healthz__"}.}

\item{rate_limit}{Optional per-IP throttle, emitted as \code{api.rate-limit},
e.g. \code{"100/min"}. Omitted when \code{NULL}.}

\item{cors}{Optional logical; when not \code{NULL}, emitted as \code{api.cors} to toggle
Ruscker's permissive CORS headers.}

\item{min_replicas}{Always-running instances, emitted as \code{min-replicas}.
Defaults to \code{0} (spawn on demand).}

\item{max_replicas}{Auto-scale ceiling, emitted as \code{max-replicas}. Defaults
to \code{3}.}

\item{env}{Optional named list/vector of \code{container-env} variables, e.g.
\code{list(AURORA_ENV = "prod")}.}

\item{wrap}{If \code{TRUE}, wrap the entry under \verb{proxy: specs:} so the output is a
complete, paste-ready snippet. If \code{FALSE} (default), emit just the
\code{- id: ...} list item to add under your existing \code{proxy.specs}.}

\item{write}{If \code{TRUE}, also write the YAML to \code{file}.}

\item{file}{Output path used when \code{write = TRUE}.}
}
\value{
The YAML block as a single string, invisibly.
}
\description{
Generates the YAML spec entry that goes under \code{proxy.specs} in a
\href{https://github.com/StrategicProjects/ruscker}{Ruscker} configuration,
pointing at a built aurora image (see \code{\link[=aurora_build_image]{aurora_build_image()}}). Ruscker is a
reverse proxy and container orchestrator (a lightweight ShinyProxy
alternative) that reads the ShinyProxy \code{application.yml} schema and adds its
own fields. An aurora app is a \emph{stateless} 'plumber2' API, so this emits a
\code{type: api} spec -- Ruscker load-balances a replica pool of the container
instead of running one container per session (contrast
\code{\link[=aurora_shinyproxy_yaml]{aurora_shinyproxy_yaml()}}, which emits the interactive Docker-backed spec).
}
\examples{
cat(aurora_ruscker_yaml(image = "org/meu_app:latest", id = "meu_app"))
}
\seealso{
\code{\link[=aurora_shinyproxy_yaml]{aurora_shinyproxy_yaml()}} for the interactive ShinyProxy spec.
}
