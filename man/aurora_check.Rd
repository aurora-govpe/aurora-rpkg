% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{aurora_check}
\alias{aurora_check}
\title{Check an aurora app for common problems}
\usage{
aurora_check(dir = ".")
}
\arguments{
\item{dir}{App directory (canonical aurora layout).}
}
\value{
Invisibly, a data frame of findings (\code{level}, \code{message}); also printed
via \pkg{cli}.
}
\description{
Static "doctor" for the canonical layout. Reports issues that otherwise
surface late (often only when building or running the container):
}
\details{
\itemize{
\item \strong{UI code in runtime helpers} -- \pkg{shiny}/\pkg{bslib}/\pkg{htmltools}
usage in \verb{helpers/} (sourced at request time) pulls the UI stack into the
runtime image. UI code belongs in \code{build_ui.R}/\verb{ui_modules/} (build time).
\item \strong{Undeclared packages} -- packages referenced in \verb{routers/}/\verb{helpers/} but
absent from \verb{_aurora.yml} \verb{packages:} (when that list is present), so the
image would miss them.
\item \strong{Missing prebuilt UI} -- no \code{www/index.html} (the container serves it and
does not rebuild).
}
}
