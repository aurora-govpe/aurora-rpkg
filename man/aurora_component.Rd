% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component.R
\name{aurora_component}
\alias{aurora_component}
\title{Wire a UI element to a JSON API endpoint}
\usage{
aurora_component(endpoint, ..., id = NULL, tag = "div")
}
\arguments{
\item{endpoint}{API path the feature JS will fetch, e.g. \code{"api/sales/data"}.
Resolved against the app base path by \code{aurora.url()} in the runtime.}

\item{...}{Passed to the underlying \pkg{htmltools} tag. Named arguments
become attributes (e.g. \code{class}, \code{style}, or extra \verb{data-*} attributes such
as \code{"data-page-size" = "25"}); unnamed arguments become child tags.}

\item{id}{Element id, so your JS can find it (\code{document.getElementById}).
Optional but recommended.}

\item{tag}{HTML tag name to emit. Defaults to \code{"div"}.}
}
\value{
An \pkg{htmltools} tag.
}
\description{
A thin markup helper: emits an \pkg{htmltools} element carrying its API
endpoint as a \code{data-endpoint} attribute (plus any extra attributes you pass).
Your app's feature JavaScript reads \code{element.dataset.endpoint}, fetches it
with the \code{window.aurora} runtime (\code{aurora.json(...)}), and renders however it
likes.
}
\details{
aurora deliberately ships no rendering JavaScript: this keeps the runtime thin
(see the design invariants in \code{CLAUDE.md} / ADR-002 and ADR-009) and leaves
charts, tables, and maps fully under the app's control. Use it to avoid
hand-writing the \verb{data-*} plumbing on every element.
}
\examples{
aurora_component("api/sales/data", id = "vendas", style = "height:360px;")
}
