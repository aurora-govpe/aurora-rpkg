% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{aurora_app}
\alias{aurora_app}
\title{Assemble an aurora app as a plumber2 API}
\usage{
aurora_app(
  dir = ".",
  rebuild_ui = TRUE,
  host = "127.0.0.1",
  port = 8000L,
  otel = NULL,
  verbose = NULL
)
}
\arguments{
\item{dir}{App directory (canonical aurora layout).}

\item{rebuild_ui}{Whether to rebuild the static UI before assembling.}

\item{host, port}{Bind address/port baked into the API object. Usually
overridden by \code{\link[=aurora_run]{aurora_run()}}.}

\item{otel}{Wire OpenTelemetry logging (\code{api_logger(logger_otel())}) so logs
join \pkg{plumber2}'s automatic request spans and metrics. \code{NULL} (default)
resolves from \verb{_aurora.yml} (\verb{otel:}) then the \code{AURORA_OTEL} environment
variable, falling back to \code{FALSE}. Wiring is a no-op until the \pkg{otel}
package is enabled in the environment, so it is safe to leave on.}

\item{verbose}{Emit a per-step \pkg{cli} log (one line per sourced helper /
parsed router, otel wiring). \code{NULL} (default) resolves from
\code{options(aurora.verbose)} then the \code{AURORA_VERBOSE} env var, falling back to
\code{FALSE} (quiet: a single assembly-summary line). Errors and warnings always
print.}
}
\value{
An object of class \code{aurora_app}.
}
\description{
Convention-based assembly: builds the UI (optional), sources \verb{helpers/*.R},
parses every \verb{routers/*.R} into a \pkg{plumber2} API, and serves the \verb{www/}
directory at \code{/}. No \verb{_aurora.yml} is required; an optional manifest only
overrides a few keys.
}
\details{
Each handler's URL is taken verbatim from its \verb{#* @get /...} annotation, so
the route prefix is embedded by \code{\link[=aurora_add_route]{aurora_add_route()}} at scaffold time -- there
is no runtime path injection here.
}
