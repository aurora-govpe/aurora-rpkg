% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-route.R
\name{aurora_add_route}
\alias{aurora_add_route}
\title{Add an API route to an aurora app}
\usage{
aurora_add_route(name, mount = NULL, dir = ".")
}
\arguments{
\item{name}{Route name; becomes \verb{routers/<name>.R}.}

\item{mount}{URL prefix for the route. Defaults to \verb{/api/<name>}.}

\item{dir}{App directory (canonical aurora layout).}
}
\value{
The created route file path, invisibly.
}
\description{
Generates an annotated \pkg{plumber2} route file under \verb{routers/}. Because
\code{aurora_app()} parses every file in \verb{routers/}, no manifest update is needed.
The handler's URL embeds the \code{mount} prefix directly in its annotation (see
ADR-005), so there is no runtime path injection.
}
