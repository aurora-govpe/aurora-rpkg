% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{aurora_auth_jwt}
\alias{aurora_auth_jwt}
\title{JWT-cookie authentication scheme}
\usage{
aurora_auth_jwt(
  secret = Sys.getenv("AURORA_JWT_SECRET"),
  cookie = "token",
  expiry = 28800L
)
}
\arguments{
\item{secret}{Secret used to sign/verify tokens (string or raw). Prefer
supplying it via an environment variable rather than hardcoding.}

\item{cookie}{Name of the cookie carrying the token.}

\item{expiry}{Token lifetime in seconds.}
}
\value{
An object of class \code{aurora_auth_jwt}.
}
\description{
aurora's auth is \emph{pluggable} and never baked into \code{\link[=aurora_app]{aurora_app()}}'s core path
(see ADR-003). This is the one provided scheme: a stateless JSON Web Token
signed with \pkg{jose} (HMAC) and delivered as an \code{HttpOnly} cookie. It is the
plumber2 translation of the reference app's v1 \verb{@filter} JWT scheme.
}
\details{
The companion helpers operate on the scheme object:
\itemize{
\item \code{\link[=aurora_jwt_token]{aurora_jwt_token()}} mints a signed token (at login).
\item \code{\link[=aurora_set_auth_cookie]{aurora_set_auth_cookie()}} / \code{\link[=aurora_clear_auth_cookie]{aurora_clear_auth_cookie()}} manage the cookie.
\item \code{\link[=aurora_jwt_guard]{aurora_jwt_guard()}} is the gate: call it from a \verb{@header} handler on
\verb{/api/*} and it rejects unauthenticated requests with a \code{401}.
}

Auth is wired entirely in your app's annotated router files (a \verb{@header}
guard + public \verb{/auth/*} routes), so \code{aurora_app()} needs no auth knowledge.
See the \code{auth} template (\code{\link[=aurora_create_app]{aurora_create_app()}}).
}
\examples{
auth <- aurora_auth_jwt(secret = "dev-only-secret")
tok <- aurora_jwt_token(auth, list(user = "alice"))
aurora_jwt_decode(auth, tok)$user
}
