% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-app.R
\name{aurora_create_app}
\alias{aurora_create_app}
\title{Scaffold a new aurora app}
\usage{
aurora_create_app(
  path,
  template = c("minimal", "dashboard", "auth"),
  engine = "plumber2"
)
}
\arguments{
\item{path}{Directory to create. Must not exist or must be empty.}

\item{template}{Bundled template: \code{"minimal"} (bare app) or \code{"auth"}
(JWT-cookie login gating \verb{/api/*}). \code{"dashboard"} is planned.}

\item{engine}{Web engine. Only \code{"plumber2"} is supported.}
}
\value{
The app path, invisibly.
}
\description{
Creates the canonical aurora layout from a bundled template: \code{api.R},
\code{build_ui.R}, \verb{helpers/}, \verb{routers/}, \verb{ui_modules/}, \verb{www/} (with \code{style.css}
and \code{js/app.js}), \code{data/config.yml}, and \code{.dockerignore}. The JS runtime is
copied fresh into \code{www/js/core.js}. A first UI build is attempted so the app
is immediately runnable.
}
