% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serializers.R
\name{aurora_unbox}
\alias{aurora_unbox}
\title{Unbox a scalar for a JSON response, NULL-safe}
\usage{
aurora_unbox(x)
}
\arguments{
\item{x}{A length-1 value, or \code{NULL}/length-0.}
}
\value{
\code{jsonlite::unbox(x)} for a scalar, or \code{NULL}.
}
\description{
Wraps \code{\link[jsonlite:unbox]{jsonlite::unbox()}} so a length-1 value serializes as a JSON scalar
instead of a 1-element array, while \code{NULL}/empty input returns \code{NULL} (which
serializes as JSON \code{null} or is dropped) instead of \verb{[]}. The \verb{[]} case is a
common footgun: a frontend doing \verb{if (x)} or \code{x[0]} misbehaves on an empty
array where it expected a scalar or null.
}
\examples{
aurora_unbox("2026-06-02")
aurora_unbox(NULL)
}
