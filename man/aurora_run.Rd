% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{aurora_run}
\alias{aurora_run}
\title{Run an aurora app locally}
\usage{
aurora_run(
  dir = ".",
  port = 8000L,
  host = "127.0.0.1",
  rebuild_ui = NULL,
  watch = FALSE,
  watch_interval = 1,
  otel = NULL,
  verbose = NULL,
  attach = NULL,
  on_exit = NULL
)
}
\arguments{
\item{dir}{App directory (canonical aurora layout).}

\item{port}{Port to bind.}

\item{host}{Host/interface to bind.}

\item{rebuild_ui}{Whether to rebuild the static UI before running. \code{NULL}
(default) resolves from the \code{AURORA_REBUILD_UI} environment variable
(default \code{TRUE} locally). Containers set it to \code{FALSE}: the UI is compiled
at build time and shipped as \code{www/index.html}, so the runtime image serves
it without the UI build dependencies (bslib, and transitively shiny).}

\item{watch}{Live-reload for development. When \code{TRUE}, aurora polls the UI
source files (\code{build_ui.R} and \verb{ui_modules/}) and rebuilds the static
\code{www/index.html} on change -- refresh the browser to see it. Changes to
\verb{routers/}/\verb{helpers/} are detected but cannot be hot-swapped into a running
server, so they log an advisory to restart. Requires the \pkg{later}
package.}

\item{watch_interval}{Polling interval in seconds when \code{watch = TRUE}.}

\item{otel}{Enable OpenTelemetry logging. Passed to \code{\link[=aurora_app]{aurora_app()}}; \code{NULL}
(default) resolves from \verb{_aurora.yml} then the \code{AURORA_OTEL} env var.}

\item{verbose}{Per-step \pkg{cli} logging. Passed to \code{\link[=aurora_app]{aurora_app()}}; \code{NULL}
(default) resolves from \code{options(aurora.verbose)} then \code{AURORA_VERBOSE}.}

\item{attach}{Attach the \verb{_aurora.yml} \verb{packages:} before sourcing helpers.
Passed to \code{\link[=aurora_app]{aurora_app()}}; \code{NULL} (default) resolves from \verb{_aurora.yml}
(\verb{attach:}) then \code{AURORA_ATTACH}. See ADR-012.}

\item{on_exit}{Optional cleanup function \verb{function()} run when the server
stops (registered on plumber2's \code{"cleanup"} lifecycle event). Use it to
release resources opened in a helper -- e.g. \code{pool::poolClose(con_base)} --
replacing the v1 \code{pr_hook("exit", ...)}. Errors in the handler are reported
but do not block shutdown.}
}
\value{
The result of \code{\link[plumber2:api_run]{plumber2::api_run()}}, invisibly.
}
\description{
Rebuilds the UI (optional), assembles the \code{\link[=aurora_app]{aurora_app()}} from convention, and
starts the \pkg{plumber2} server. Development-time equivalent of
\code{shiny::runApp()}. The generated \code{api.R} calls this function, so local dev
and container entry share one assembly path.
}
