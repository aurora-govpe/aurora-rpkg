% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app-config.R
\name{aurora_config}
\alias{aurora_config}
\title{Read the app's \code{data/config.yml}, anchored to the app root}
\usage{
aurora_config(
  value = NULL,
  ...,
  dir = ".",
  file = NULL,
  config = Sys.getenv("R_CONFIG_ACTIVE", "default")
)
}
\arguments{
\item{value}{Config value to read; \code{NULL} (default) returns the whole active
configuration as a list.}

\item{...}{Passed to \code{\link[config:get]{config::get()}}.}

\item{dir}{App directory (used to locate \code{data/config.yml}).}

\item{file}{Explicit path to the config file; overrides \code{dir} when supplied.}

\item{config}{Active configuration name; defaults to the \code{R_CONFIG_ACTIVE}
environment variable or \code{"default"}.}
}
\value{
The requested config value (or the whole config list).
}
\description{
Thin wrapper over \code{\link[config:get]{config::get()}} that resolves \code{data/config.yml} relative to
the \strong{app directory} (an absolute path), instead of the \pkg{config} package's
default search from the current working directory. This avoids the cwd pitfall
where a helper or handler is evaluated with a working directory other than the
app root and \code{config::get()} cannot find the file.
}
\details{
\code{data/config.yml} (app runtime config: DB credentials, environment profiles,
service URLs) is intentionally separate from \verb{_aurora.yml} (aurora wiring); see
the project decision records. This helper just makes reading it robust.
}
