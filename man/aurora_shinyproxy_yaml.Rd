% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shinyproxy.R
\name{aurora_shinyproxy_yaml}
\alias{aurora_shinyproxy_yaml}
\title{Emit a ShinyProxy app-spec block for an aurora image}
\usage{
aurora_shinyproxy_yaml(
  image,
  dir = ".",
  id = NULL,
  display_name = NULL,
  description = NULL,
  port = 8000L,
  env = NULL,
  wrap = FALSE,
  write = FALSE,
  file = "shinyproxy-app.yml"
)
}
\arguments{
\item{image}{Container image tag, e.g. \code{"org/meu_app:latest"} (required).}

\item{dir}{App directory, used only to default \code{id}/\code{display_name} from the
app name.}

\item{id}{Spec id. Defaults to the app name.}

\item{display_name}{Human-facing name. Defaults to the app name.}

\item{description}{Optional one-line description.}

\item{port}{Port the app listens on inside the container (the aurora default
is \code{8000}).}

\item{env}{Optional named list/vector of \code{container-env} variables, e.g.
\code{list(AURORA_ENV = "prod")}.}

\item{wrap}{If \code{TRUE}, wrap the entry under \verb{proxy: specs:} so the output is a
complete, paste-ready snippet. If \code{FALSE} (default), emit just the
\code{- id: ...} list item to add under your existing \code{proxy.specs}.}

\item{write}{If \code{TRUE}, also write the YAML to \code{file}.}

\item{file}{Output path used when \code{write = TRUE}.}
}
\value{
The YAML block as a single string, invisibly.
}
\description{
Generates the YAML spec entry that goes under \code{proxy.specs} in a ShinyProxy
configuration, pointing at a built aurora image (see \code{\link[=aurora_build_image]{aurora_build_image()}}).
aurora apps are ordinary Docker-backed apps from ShinyProxy's point of view:
the spec just needs the image and the port the app listens on.
}
\examples{
cat(aurora_shinyproxy_yaml(image = "org/meu_app:latest", id = "meu_app"))
}
