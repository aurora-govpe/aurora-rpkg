% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-store.R
\name{aurora_data_store}
\alias{aurora_data_store}
\title{Create a hot-reloading data store}
\usage{
aurora_data_store(..., dir = ".", readers = list())
}
\arguments{
\item{...}{Named file paths to register (e.g. \code{sales = "data/sales.rds"}).
The reader is inferred from the file extension.}

\item{dir}{Base directory that relative paths are resolved against, at read
time. Defaults to \code{"."} (the process working directory). In the canonical
deployment the app is launched from its own directory (\verb{Rscript api.R}, or
\code{aurora_run()} from the app dir), so relative paths like \code{"data/x.rds"}
resolve correctly. Pass an absolute path if you cannot rely on the working
directory. Absolute dataset paths are used as-is regardless of \code{dir}.}

\item{readers}{Named list of reader functions keyed by lowercase file
extension, merged over (and overriding) the built-ins (\code{rds}, \code{csv},
\code{parquet}).}
}
\value{
An object of class \code{aurora_data_store}.
}
\description{
Registers named datasets backed by files on disk and hands them to route
handlers without globals or \verb{<<-}. Each \code{\link[=aurora_data_get]{aurora_data_get()}} checks the file's
modification time and transparently re-reads it if an external process (e.g.
an ETL job) has rewritten it -- the stateless equivalent of the reference
app's \code{carregar_bases()} mtime trick.
}
\details{
Define the store once in a \verb{helpers/*.R} file (sourced before routers are
parsed) and read from it in handlers:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# helpers/data.R
store <- aurora_data_store(sales = "data/sales.rds", dir = ".")

# routers/sales.R
#* @get /api/sales
#* @serializer json
function() aurora_data_get(store, "sales")
}\if{html}{\out{</div>}}
}
\examples{
f <- tempfile(fileext = ".rds")
saveRDS(data.frame(x = 1:3), f)
store <- aurora_data_store(demo = f)
aurora_data_get(store, "demo")
}
